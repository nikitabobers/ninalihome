import React from "react";
import Loader from "../layout/loader/Loader";

const Home = () => {
    return (
        <div>
            <h1>Home page</h1>
            <Loader />
        </div>
    );
};

export default Home;
