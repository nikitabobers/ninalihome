import React from "react";
import SliderItem from "./sliderItem/SliderItem";

const Slider = () => {
    return (
        <div>
            <SliderItem />
        </div>
    );
};

export default Slider;
